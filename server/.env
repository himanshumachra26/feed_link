# Environment variables for FeedLink server
# Replace the placeholder values with your actual credentials before deploying.

# Secret used for signing JWT tokens. Keep this secret safe!
JWT_SECRET=feedlink-dev-secret

# Database connection URL for Prisma. Example for PostgreSQL:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
DATABASE_URL="file:./dev.db"
